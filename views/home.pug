doctype html
html
    head
    title Home
    style 
        include ./home_style.css
    body
        h1(style="text-align: center") Check-in/Check-out system
        div(class="container")
            div(class="info-wrap")
                div(class="info")
                    p Name: #{fullName}
                    p Balance: #{balance}
                    button(type="button", onclick="topUp()").btn Top up
                div(class="temp")
                div
                    a(href="/user/logout")
                            button(type='submit').btn.btnlogout Logout
            table(class="table") 
                tr 
                    th ID
                    th Location 
                    th Time
                    th Fee  
                each value, index in history
                    tr
                        th #{index + 1}
                        th #{value.location}
                        th #{value.time}
                        th #{value.fee}

    script(type='text/javascript').
        console.log("Hello world")
        function topUp() {
            fetch("/user/topup").then(res => res.json())
            .then(data => {
            const {message} = data
            alert(message)
            })
        }
